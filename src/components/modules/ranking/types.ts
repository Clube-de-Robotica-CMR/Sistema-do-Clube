export interface RankingRow {
    member_number: string;
    name: string;
    gold_count: number;
    silver_count: number;
    bronze_count: number;
    total_points: number;
    can_ascend: boolean;
}